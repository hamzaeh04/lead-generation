# Local development
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Production (Vercel) — leave unset to use the built-in default, or set explicitly:
NEXT_PUBLIC_API_BASE_URL=https://lead-generation-backend-nu.vercel.app
